//export let name = 'Max';

export function naimenshee (a, b) {
    if (a < b) {
      return a;
    } else {
      return b;
    }
  }
  //export let vivod = naimenshee (a, b);
  //console.log(function)